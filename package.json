{
  "name": "domeo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A small, offline-friendly woodland survival game built with HTML5 Canvas.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.8"
  },
  "engines": {
    "node": ">=22"
  }
}
